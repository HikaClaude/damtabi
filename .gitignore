# 観測所マスタのキャッシュ（取得回数削減用。再生成できるので追跡しない）
cache/

# 作業中の一時ファイル
docs/_*.html
*.bak

# ピン案などの検討用ページ（公開サイトには含めない。残しておくと再検討が楽）
# lab/ は追跡する。生成物だけ除外したい場合はここに追記する

# 標高タイルのキャッシュ（再取得できるので追跡しない。約1000枚・数百MB）
cache/dem/

# 集水域の計算結果。第2段階で表示すると決まるまで docs/ には出さない
data/basins/*.log

__pycache__/
